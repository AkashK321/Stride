repo
--app (frontend)
--test (selenium)
--assets
--aws (backend)
--tests (unit integration)
--utilities
--README
--docs
--requirements.txt
--gitignore

BRANCHING

- Use the issue branches from Git
  - Delete once the issue is solved
  - Branch naming: issue-name-#issuenum
- Another team member needs to review & approve the PR before the merge
- Have 1 main branch with whatever feature branches are being worked on
