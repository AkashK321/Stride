.venv/
.env.inference
.cache/
__pycache__/
*.pyc
.pytest_cache/

# Local runtime persistence (single-machine): SQLite dashboard + invocation artifacts.
# Models under .data/models/ are intentionally tracked (custom weights).
.data/artifacts/
.data/models/
.data/*.sqlite3
